{
  "name": "silently.io",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=silent-app",
    "dev:serve": "npm run serve --workspace=silent-server"
  },
  "workspaces": [
    "silent-app",
    "silent-server",
	"silent-mobile"
  ],
  "devDependencies": {
    "jest": "^27.0.6",
    "supertest": "^6.1.4"
  }
}
